// backend/package.json
{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend per l'app di inventario domestico",
  "main": "src/app.js", // Modificato per puntare a src/app.js
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js", // Script per avviare con nodemon
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0", // La versione potrebbe variare
    "express": "^4.18.0", // La versione potrebbe variare
    "multer": "^1.4.5-lts.1",
    "pg": "^8.7.0",       // La versione potrebbe variare
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.20.0" // La versione potrebbe variare
  },
  "devDependencies": {
    "nodemon": "^2.0.15", // La versione potrebbe variare
    "sequelize-cli": "^6.4.0" // La versione potrebbe variare
  }
}
